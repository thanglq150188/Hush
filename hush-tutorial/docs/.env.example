# =============================================================================
# Hush - Environment Variables
# =============================================================================
# Copy file này thành .env ở thư mục gốc project:
#   cp docs_v2/.env.example .env
#
# Sau đó điền API keys của bạn vào.
# =============================================================================

# -----------------------------------------------------------------------------
# Hush Config
# -----------------------------------------------------------------------------
# Đường dẫn đến file resources.yaml (nếu không set, Hush sẽ tìm theo thứ tự:
# ./resources.yaml → ~/.hush/resources.yaml)
# HUSH_CONFIG=./resources.yaml

# Đường dẫn lưu traces (LocalTracer dùng SQLite)
# HUSH_TRACES_DB=~/.hush/traces.db

# -----------------------------------------------------------------------------
# OpenAI
# -----------------------------------------------------------------------------
# Đăng ký tại: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-proj-your-key-here

# -----------------------------------------------------------------------------
# OpenRouter
# -----------------------------------------------------------------------------
# Đăng ký tại: https://openrouter.ai/keys
# OpenRouter cho phép truy cập nhiều model (Claude, GPT, Llama...) qua 1 API key
OPENROUTER_API_KEY=sk-or-v1-your-key-here

# -----------------------------------------------------------------------------
# Langfuse (Observability)
# -----------------------------------------------------------------------------
# Đăng ký tại: https://cloud.langfuse.com
# Vào Settings → API Keys để lấy public/secret key
LANGFUSE_PUBLIC_KEY=pk-lf-your-key-here
LANGFUSE_SECRET_KEY=sk-lf-your-key-here
LANGFUSE_HOST=https://cloud.langfuse.com

# -----------------------------------------------------------------------------
# Logging (tuỳ chọn)
# -----------------------------------------------------------------------------
# LOG_LEVEL=WARNING
# LOG_USE_RICH=auto
